name: Dataset
on:
  push:

jobs:
  Deploy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'skip dataset')"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Setup python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v2
        with:
          path: .venv
          key: venv-dataset-${{ runner.os }}-${{ hashFiles('**/poetry.lock') }}

      - name: Install dependencies
        run: poetry install --no-root
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'

      - name: Generate Hash
        run: |
          source .venv/bin/activate
          python3 pipelines/ci_hash_dataset.py

      - name: Deploy
        uses: s0/git-publish-subdir-action@develop
        env:
          REPO: git@github.com:noaione/vthell-dataset.git
          BRANCH: master
          FOLDER: dataset
          SSH_PRIVATE_KEY: ${{ secrets.DEPLOY_PRIVATE_KEY }}
          MESSAGE: "🔁 Dataset update from @ noaione/vthell@{long-sha}"
